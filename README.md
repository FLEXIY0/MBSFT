# 🎮 MBSFT — Minecraft Beta Server For Termux

[![Version](https://img.shields.io/badge/version-2.4-blue.svg)](https://github.com/FLEXIY0/MBSFT)
[![License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)
[![Platform](https://img.shields.io/badge/platform-Termux-orange.svg)](https://termux.com)

Мульти-сервер менеджер для **Minecraft Beta 1.7.3** на Android через Termux. Создавай сколько угодно серверов, каждый в своей папке, и управляй ими из одного меню.

![MBSFT Demo](https://via.placeholder.com/800x400?text=MBSFT+Demo)

---

## ✨ Возможности

- 🎮 **Мульти-сервер** — неограниченное кол-во серверов, каждый со своим именем, портом и настройками
- 🖥️ **Dialog UI** — удобные меню, ввод, подтверждения прямо в терминале
- 📊 **Дашборд** — статус всех серверов в одном месте
- 🔌 **Автоподбор порта** — 25565, 25566, 25567... без конфликтов
- ⚡ **Быстрое создание** — ввёл имя → скачалось ядро → настроилось → готово
- 🔄 **Сервисы** — автозапуск + автосохранение каждые 10 мин
- 🔐 **SSH** — управление сервером с ПК
- 🔃 **Автообновление** — скрипт сам обновляется до новой версии
- 🧩 **Модульная архитектура** — легко добавлять новые функции

---

## 🏗️ Архитектура

**Версия 2.4** использует модульную архитектуру с самораспаковкой:

```
install.sh (самораспаковывающийся)
    ↓
~/.mbsft/modules/
    ├── core.sh      # Базовые функции
    ├── java.sh      # Управление Java 8
    ├── server.sh    # Управление серверами
    └── ui.sh        # Dialog интерфейс
```

**Преимущества:**
- ✅ Один файл для установки
- ✅ Модульный код для удобной разработки
- ✅ Автоматическое обновление всех компонентов
- ✅ Исправлена проблема с зависшими процессами при удалении серверов

---

## 📦 Установка

### Быстрая установка (рекомендуется)

Откройте Termux и выполните:

```bash
curl -sL https://raw.githubusercontent.com/FLEXIY0/MBSFT/main/install.sh | bash
```

**Скрипт автоматически:**
- Скачается в `~/install.sh`
- Запустится
- При каждом последующем запуске будет проверять обновления

### Альтернатива через wget

```bash
pkg install wget
wget https://raw.githubusercontent.com/FLEXIY0/MBSFT/main/install.sh
bash install.sh
```

### Повторный запуск и автообновление

После первой установки скрипт сохраняется в `~/install.sh`:

```bash
bash install.sh
```

**Автообновление:** При каждом запуске скрипт автоматически проверяет наличие новой версии на GitHub и обновляется при необходимости.

---

## 🚀 Использование

### Быстрый старт

1. Запустить скрипт: `bash install.sh`
2. **Установить зависимости** (первый пункт меню)
3. **Быстрое создание** → ввести имя, например `survival`
4. Готово! Управляй через **Мои серверы**

### Главное меню

```
┌─────────── MBSFT v2.4 ───────────┐
│ Java: OK | Серверов: 2 | Запущено: 1 │
│                                       │
│   Установить зависимости              │
│   Создать сервер (пошагово)           │
│   Быстрое создание                    │
│   Мои серверы [2 шт.]                 │
│   Дашборд                             │
│   Настроить SSH                       │
│   Выход                               │
└───────────────────────────────────┘
```

### Управление сервером

```
┌──── [РАБОТАЕТ] survival ────┐
│ RAM: 1G | Порт: 25565           │
│                                  │
│   Запустить                      │
│   Остановить                     │
│   Перезапустить                  │
│   Консоль (screen)               │
│   Настройки (RAM, порт)          │
│   Создать сервис (автосохр.)     │
│   Удалить сервер                 │
└──────────────────────────────┘
```

### Подключение к серверу

1. **Дашборд** покажет IP и порт каждого сервера
2. В Minecraft Beta 1.7.3 → Multiplayer → Direct Connect:
   ```
   192.168.1.42:25565
   ```
3. Оба устройства должны быть в одной Wi-Fi сети

---

## 📁 Структура проекта

```
~/mbsft-servers/
├── survival/              # сервер "survival"
│   ├── .mbsft.conf        # конфиг (RAM, порт, ядро)
│   ├── server.jar
│   ├── start.sh
│   ├── server.properties
│   └── ...
├── creative/              # сервер "creative"
│   └── ...
└── pvp-arena/             # сервер "pvp-arena"
    └── ...
```

---

## 🔧 Технические детали

- **Java:** OpenJDK 8 (автоматическая установка через [Termux-Java](https://github.com/MasterDevX/Termux-Java))
- **Автообновление:** Встроенная система проверки версий при каждом запуске
- **Изоляция:** Каждый сервер в отдельной директории
- **Управление:** Dialog TUI + Screen sessions
- **Модули:** Самораспаковывающаяся архитектура

### Что нового в v2.4

- ✅ **Исправлено:** Серверы теперь принудительно останавливаются перед удалением (нет зависших процессов)
- ✅ **Модульная архитектура:** Код разбит на логические модули для удобства разработки
- ✅ **Улучшенное автообновление:** Проверка версии через переменную `MBSFT_VERSION`
- ✅ **Java 8 only:** Полная поддержка только Java 8, убрана поддержка Java 17

---

## 🎯 Ядра серверов

По умолчанию — **Project Poseidon** (Beta 1.7.3, исправления багов, поддержка плагинов).

Своё ядро: при создании сервера выбери "закину сам" и положи `server.jar` в папку сервера.

---

## 🐛 Решение проблем

| Проблема | Решение |
|----------|---------|
| `Java not found` | Перезапусти Termux, выбери "Установить зависимости" |
| Сервер не стартует | `cat ~/mbsft-servers/<имя>/eula.txt` — должно быть `eula=true` |
| Не подключается | Оба устройства в одной Wi-Fi? Порт правильный? |
| Конфликт портов | Каждый сервер должен быть на своём порту (авто-подбор это решает) |
| Мало RAM | Настройки сервера → поставь `512M` |
| Скрипт не обновляется | Проверь интернет-соединение, попробуй `rm ~/install.sh` и установи заново |

---

## 👨‍💻 Для разработчиков

См. [DEVELOPER.md](DEVELOPER.md) для подробной информации об архитектуре и разработке.

### Быстрая сборка

```bash
# Изменить модули в modules/
nano modules/server.sh

# Пересобрать install.sh
./build.sh

# Протестировать
bash install.sh
```

---

## 📋 Требования

- Android устройство
- [Termux](https://f-droid.org/packages/com.termux/) (через F-Droid, НЕ Google Play)
- Wi-Fi (для игры по локальной сети)
- ~200 MB свободного места

---

## 🤝 Вклад в проект

Приветствуются Pull Requests! Пожалуйста:

1. Форкните репозиторий
2. Создайте ветку для фичи (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

---

## 📜 Лицензия

MIT License - см. [LICENSE](LICENSE)

---

## 🙏 Благодарности

- [Project Poseidon](https://github.com/RhysB/Project-Poseidon) — серверное ядро
- [MasterDevX/Termux-Java](https://github.com/MasterDevX/Termux-Java) — установщик Java 8
- [Termux](https://termux.com) — эмулятор терминала для Android

---

## 📞 Контакты

- **GitHub:** [FLEXIY0/MBSFT](https://github.com/FLEXIY0/MBSFT)
- **Issues:** [Сообщить о проблеме](https://github.com/FLEXIY0/MBSFT/issues)

---

<p align="center">
  Сделано с ❤️ для сообщества Minecraft
</p>
